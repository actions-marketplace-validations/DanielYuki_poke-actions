name: "Poke Actions"
description: "Every time someone closes an issue or pull request, they catch a random Pokémon"
author: "Daniel Yuki"
branding:
  icon: 'git-commit'
  color: 'red'
inputs:
  github_token:
    description: "GitHub token for commenting on issues and pull requests"
    required: true
runs:
  using: "node20"
  main: "dist/index.js"
