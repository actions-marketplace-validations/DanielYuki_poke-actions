{
  "name": "poke-actions",
  "version": "1.0.0",
  "description": "Every time someone closes an issue or pull request, they catch a random Pokémon",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build lib/index.js -o dist --source-map --license licenses.txt",
    "dev": "tsc --watch",
    "package": "pnpm run build"
  },
  "keywords": ["github-actions", "pokemon", "issues", "pull-requests", "fun"],
  "author": "Daniel Yuki",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.3"
  }
}
